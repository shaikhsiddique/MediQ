MONGODB_URI=mongodb://127.0.0.1:27017/mediq
# Optional: target a specific patient (otherwise uses first patient in DB)
PATIENT_EMAIL=patient@example.com
# How many dummy reports to create (default 5)
DUMMY_REPORT_COUNT=5
